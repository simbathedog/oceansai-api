import {readFileSync} from 'fs';import {resolve} from 'path';import {Client} from 'pg';
async function main(){const url=process.env.DATABASE_URL;if(!url)throw new Error('DATABASE_URL is not set');const sql=readFileSync(resolve(process.cwd(),'db','schema.sql'),'utf8');const client=new Client({connectionString:url,ssl:{rejectUnauthorized:false}});await client.connect();try{await client.query(sql);console.log('Schema applied ✔');}finally{await client.end();}}main().catch(e=>{console.error('Migration failed:',e);process.exit(1);});